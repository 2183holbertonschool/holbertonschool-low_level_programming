#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* postitive_or_negative - entry pointa any Number
* Return: always 0 (success)
**/
void postitive_or_negative(int n)
{
	printf("%i is positive\n", n);
	}
if (n == 0)
{
printf("%i is zero\n", n);

			if (n < 0)
			{
				printf("%i is negative\n", n);
				}
				return (0);
}